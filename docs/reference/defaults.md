# Predictable defaults

| Profile | Behavior |
| --- | --- |
| `fast` | Local AST chunks and TF-IDF retrieval |
| `quality` | Optional vectors and reranking when configured |
| Scope | Generated files, dependencies, and build output are excluded |

No Docker, vector database, or API key is required for the default path.
