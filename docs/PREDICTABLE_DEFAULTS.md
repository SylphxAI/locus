# Predictable defaults

`fast` uses local AST chunks and TF-IDF. `quality` enables optional vectors and
reranking when configured. No Docker, vector database, or API key is required for
the default path.
